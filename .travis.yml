sudo: required

services:
  - docker

before_install:
  - sudo pip install docker-compose

before_script:
  - docker-compose up --build -d
  - sleep 15

script:
  - ./test.sh
  - ./test-es-v2.sh

after_script:
  - docker-compose down
